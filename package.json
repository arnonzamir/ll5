{
  "name": "ll5",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:shared": "npm run build --workspace=packages/shared",
    "dev": "npm run dev --workspaces --if-present",
    "clean": "rm -rf packages/*/dist",
    "typecheck": "for pkg in packages/shared packages/ll5-auth packages/personal-knowledge packages/gtd packages/awareness packages/google packages/messaging packages/health packages/system packages/gateway packages/dashboard; do echo \"== $pkg ==\"; npx tsc --noEmit --project \"$pkg/tsconfig.json\" || exit 1; done",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
