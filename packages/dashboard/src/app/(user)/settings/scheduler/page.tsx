import { SchedulerSettingsView } from "./scheduler-settings-view";

export const metadata = { title: "Scheduler Settings - LL5" };

export default function SchedulerSettingsPage() {
  return <SchedulerSettingsView />;
}
