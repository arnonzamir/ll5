"use client";

/**
 * Chat widget placeholder — will connect to gateway /chat endpoints.
 * Currently renders a collapsed button that expands to a simple chat interface.
 */
export function ChatWidget() {
  // Placeholder for future chat implementation
  return null;
}
