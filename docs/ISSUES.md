# LL5 Issue Register

The single living list of known defects and degradations. Replaces the old `## Known Issues` bullets in `PROGRESS.md`.

**Working rule:** closing an issue means flipping its `Status` here **and** adding the normal dated `## YYYY-MM-DD —` entry to `PROGRESS.md` in the same commit, with the commit hash in `Closed by`. The pre-commit hook only checks that the three living docs are staged; this file is what keeps state honest.

Statuses: `open` · `in-progress` · `fixed` (shipped, not yet verified live) · `verified` · `wontfix` (with reason).

Origin: the 2026-09-04 agent review (`docs/reviews/2026-09-04/agent-baseline.md`, plan and phase status in `docs/implementation/agent-remediation-2026-09-04.md`). Evidence queries are against the prod ES indices unless noted.

---

## Register

| ID | Class | Sev | Title | Status | Closed by |
|---|---|---|---|---|---|
| ISS-001 | telemetry | high | Eval recorder over-counts deliveries (root cause: span carry-over across quiet turns — see detail) | fixed | agent `b6c896f`, live 16:22Z; verify: no `ping_now` without a delivery tool over 7 days |
| ISS-002 | knowledge | high | `note_observation` near-dead: 18 calls in 15 days, zero Aug 23–31; three-month drift 963 → 11/month | in-progress | Phase 2 shipped 2026-09-04: CLAUDE.md `or` → `and`, consolidate Step 1.4 observations pass + CONSOLIDATE-TALLY, `knowledge.observations_stale` check; verify: daily observation count > 0 for 7 days |
| ISS-003 | knowledge | high | Narrative consolidation silent 12 days — starved by ISS-002, not a loop fault | in-progress | no code change needed: resolves when ISS-002's observations flow (the loop is evidence-driven); verify at the 2026-09-11 re-measure: `consolidate_narrative` calls > 0 on ≥5 of 7 days |
| ISS-004 | behavior | high | `ping_later` books nothing: 57 of 72 claims hollow | fixed | 2026-09-05 (DECISION-030 batch B): `record_moment` refuses `decision: ping_later` without `deferral_ref` (the `create_wake`/`create_tickler` id booked this turn); `deferral_ref` shipped in `ll5_eval_moments`. Verify at the 09-12 readout: `ping_later` moments all carry `deferral_ref`; hollow rate → 0 |
| ISS-005 | telemetry | med | No idempotency on `/telemetry/eval-moment` and `/telemetry/turn-cost` writes | fixed | 2026-09-04 gateway Phase 1 commit |
| ISS-006 | telemetry | med | `ll5_turn_costs` dead since 2026-07-13 | fixed | writer live 16:23Z + `telemetry.turn_costs_stale` (12h) shipped 2026-09-04 evening; verify: check quiet for 7 days |
| ISS-007 | provenance | high | Live CLI 2.1.197 ≠ Dockerfile pin 2.1.204; running commit unverifiable | verified | 2026-09-04 16:22Z — rolled container: `claude version OK: 2.1.204 == pin 2.1.204`. Phase 5 (2026-09-05 checkpoint): pin → 2.1.260 in `docker/Dockerfile.ll5-run-claude` **and** the `build-and-push.yml` build-arg fallback (`vars.CLAUDE_CODE_VERSION || …` — the workflow value wins over the Dockerfile ARG; keep both aligned), model → `claude-opus-5`; **verified 2026-09-05 00:37Z** in the rolled container (`claude version OK: 2.1.260 == pin 2.1.260`, transcript model `claude-opus-5`) |
| ISS-008 | scaffolding | med | Reconcile subsystem: 682 selector calls, 0 actions, `candidate_count:0` with 5 indistinguishable causes | fixed | retired, DECISION-028 #1 (2026-09-04, commit `refactor(scaffolding): retire the reconcile subsystem (DECISION-028 #1)`); agent-repo half (`reconcile-loop.sh`, `.mcp.reconcile.json`, entrypoint launch) is the agent PR |
| ISS-009 | scaffolding | med | Two divergent narrative-freshness policies; two copies of the reconcile selector + gate | fixed | both halves 2026-09-04: reconcile copies retired (DECISION-028 #1), `NarrativeConsolidationScheduler` + its policy deleted (#2) |
| ISS-010 | scaffolding | med | 76.5% of tool calls are housekeeping; 32 gateway schedulers + 2 in-container loops | in-progress | DECISION-028 batch 1 shipped 2026-09-04 evening: reconcile retired, NarrativeConsolidation + JournalHealth retired, expiry sweeps merged, HealthPolling gated, alert cadence 6h/24h, dashboard poll 5 min, narrative loop gated, health MCP out of the agent surface; re-measure at +7d |
| ISS-011 | knowledge | med | Journal backlog: 1,229 `context` entries open over 7 days | open | measure first: the nightly `CONSOLIDATE-TALLY resolved=<n>` vs ~300/day intake; raise the consolidate resolve budget in the next persona batch only if it does not converge |
| ISS-012 | knowledge | med | User-model history dead since 2026-06-20: `ll5_agent_user_model_history` hit ES's 1000-field mapping limit, every snapshot rejected inside a bare `catch {}` | fixed | index recreated with `content: enabled:false` (148 versions kept, 2026-09-04); awareness declares the mapping + logs/reports snapshot failures (`fix(awareness): user-model history index mapping explosion (ISS-012)`); verify: a history doc with `archived_at` after 2026-09-04T23:00Z after tonight's `write_user_model` |
| ISS-013 | infra | med | Chronic unfixed: WA bridge stalls, Gmail/Slack mirror listeners, Google OAuth disconnects, TS_AUTHKEY lapsed, agent CI cold since Jul 14 | in-progress | deploy path rebuilt (DECISION-027). **WA "bridge stall" fixed 2026-09-05:** it was the flow monitor calling night silence an outage (14 raises in 14 days, all 01:07–05:21 local; Evolution showed no fault) — now ground-truth bridge liveness (every Evolution event recorded in `utils/whatsapp-bridge-liveness.ts`) + `connection.update` state + data-driven quiet hours; verify: no `channel.whatsapp` raise tonight, `reason` in the admin flow snapshot. Gmail/Slack listeners + OAuth disconnects remain |
| ISS-014 | telemetry | high | `POST /sessions` exceeds the gateway 1 MB body cap past ~250 messages → 413 swallowed → `ll5_session_history` frozen per session | verified | agent hook `b6c896f` live 16:22Z: `session-save.log` `ok http=201 append msgs=4/4`, new session doc advancing |
| ISS-015 | behavior | high | Post-compaction re-ground reads the frozen session index | verified | session doc advances every Stop from the new container (16:23:55Z); re-ground now reads live data |
| ISS-016 | scaffolding | med | Nothing monitors session age, compaction cadence, or session-save liveness; sessions roll only on container restart | verified | mechanism verified 2026-09-04 17:03Z: manual `restart-requested` → `FRESH RESTART (requested by consolidate (idle))` → new session `df0836fc`, `session-restart` journal entry, agent working; first nightly hand-off runs 2026-09-05 02:00 local (`agent.daily_restart_missing` will say if it doesn't) |
| ISS-017 | knowledge | med | Governed memory-capture path idle: `ingest_memory` 0 since July, `upsert_lesson` 0 in Sep, `recall_lessons` 7,625 in Aug | in-progress | read side fixed 2026-09-04 (context pack replaced the per-prompt `recall_lessons`; fail-closed + outbox + autoheal drain live `b6c896f`); write side — a lesson-promotion step in `skills/consolidate/SKILL.md` — is in the next persona batch |
| ISS-018 | knowledge | high | Agent bypasses ES via `Bash` grep/python over spilled `tool-results/mcp-awareness-*.txt` files | fixed | `spill-read-block.sh` wired (PreToolUse Bash\|Read\|Grep\|Glob), live 16:22Z; verify: zero spill reads over 7 days |
| ISS-019 | knowledge | high | Unbounded MCP read results (`read_journal` ~60 KB, `recall_everything` up to 114 KB, one at 1.7 MB) cause the spill | verified | live 2026-09-04 ~19:55Z: `read_journal(limit:100)` → 19,599 chars, 23 items, `truncated:true` + cursor + hint; 0 spill files since the roll |
| ISS-020 | behavior | med | Deferred tool schemas: knowledge-write tools absent from the post-compaction reflex set | fixed | core-tools block in `session-start.sh`, live 16:22Z; verify: `ToolSearch` for the core set on each startup/compact |
| ISS-021 | knowledge | med | ~20 MCP `-32602` input-validation failures across 10 tools; `note_observation` 2 of 13 | verified | live 2026-09-04: `list_horizons({})` accepted (tests lock the other 8 payloads); persona field names shipped with Phase 2 |
| ISS-022 | behavior | low | `record_moment` is 53% of main-session tool calls | wontfix | re-rated 2026-09-05: the call is local and network-free, and it is now the ISS-004 enforcement point (DECISION-030 batch B); the sentinel plan (DECISION-028 #5) is superseded |
| ISS-023 | infra | med | CI: a push to main that touches **no** package (docs-only) rebuilds and redeploys all 10 infra services | verified | docs-only push `f2ba560` → run 33895111679: `build: skipped`, `deploy: skipped` |
| ISS-025 | infra | high | CI push diff used `HEAD~1..HEAD`: a push whose tip is a merge commit rebuilt only the tip's first-parent delta — Track B's five MCP changes deployed only `gateway` | fixed | 2026-09-04 — diff against `github.event.before` (fetch-depth 0), fallback HEAD~1; the skipped five re-dispatched by hand |
| ISS-024 | scaffolding | high | Orchestrator had no `agentTokenResolver` (defaulted to `() => null`): the stale-heartbeat **restart path never once worked**, and the DECISION-027 image roll skipped with "no agent token" | verified | `a0e5deb` deployed; 16:22:14Z roll: `reprovisioned:1` — new container on the pinned image |
| ISS-026 | scaffolding | med | Context-cap fresh restarts every ~2h: the context floor right after the first turn is ~80K (system prompt + persona + start pack), +~850 tokens per assistant message, cap 120K → ~9 user turns per session; a cap restart at 22:32Z delayed the first nightly hand-off ~1.5h behind the 2h anti-flap gap | fixed | 2026-09-05 checkpoint (mid-level decision, reviewed): cap 150K, gap 1h, `context_tokens()` now counts `cache_creation_input_tokens`; shipped in the Phase 5 roll; verify 2026-09-06: sessions ≥4h, `grep -c isCompactSummary` = 0 on the transcript, nightly flag consumed within 1h |
| ISS-027 | infra | **critical** | 3h40m outage 2026-09-05 03:41–07:20Z: after the first context-cap relaunch on Claude Code 2.1.260 the blind `Down Enter` startup-picker dismissal selected "2. Exit" on the dev-channels picker (its default moved to the good option); claude exited, the container restart-looped, the container heartbeat kept beating, first alert 1.5h later, push to Arnon ~5h later | fixed | agent `0fa8ea7` (pointer-aware dismissal, hot-patched 07:20Z, image rolled 07:23Z: `picker dismissal done after 3 polls`); liveness follow-up: heartbeat carries `claude_alive`/`launches_10m` → `agent.process_down` (3 min) / `agent.launch_loop` (≥3 launches/10 min), symptom checks `suppressedBy` the cause (gateway migration 045, `utils/agent-liveness.ts`) |
| ISS-028 | infra | high | Messaging MCP: `read_messages` returned `[]` for every WhatsApp conversation (Evolution v2 `{messages:{records}}` envelope read as an array) and the service ran with no `ELASTICSEARCH_URL` → zero `source: messaging` audit rows, both silent; reported by the agent 2026-09-05 ("disconnected-then-empty") | fixed | `fetchMessages` unwraps `.records` (+ tests), compose env added, `initAudit('')` now logs loudly; **verified 2026-09-05 07:50Z:** `read_messages(platform:whatsapp, group)` → 3 rows, latest 07:40:50Z; messaging boots with `ELASTICSEARCH_URL` set and no `[audit] … DISABLED` line (audit rows appear on the next mutation, e.g. a send — reads are not audited) |
| ISS-029 | infra | high | Agent parked on the dev-channels picker for 40 min after the deploy smoke's forced relaunch (12 pending triggers, a user message unanswered 4 min): the pointer-aware dismisser stopped after one poll because the PREVIOUS claude's "shift+tab to cycle" line was still on the tmux pane, and the smoke's `agent_up` was fooled by the same stale text | fixed | 2026-09-05 (agent `ll5-server`/`docker-entrypoint.sh`: clear the screen before launch, handle a visible picker before testing for the chat input, two clean polls to stop; workflow smoke rejects a visible picker; heartbeat `picker_visible` → `agent.picker_stuck` critical after 3 min, migration 047). **Verified 2026-09-05 12:13Z** (run 33965254689): smoke `fresh boot OK after 40s`, `forced relaunch OK after 10s` with the picker-aware check, `picker dismissal done after 4 polls`, first session-save 2 min after the roll, heartbeat `claude_alive: true`, triggers draining |

---

## Detail

### ISS-001 — Eval recorder over-counts deliveries
- **2026-09-04 ROOT CAUSE CORRECTED (from the Aug 21 local eval log):** 166 of the 183 `ping_now` records that day carried **no delivery tool at all** — every one had `message_sent: "Nightly consolidation done…"`, the same push, repeated on Stops at :01/:11/:21 for hours. The `reply`-channel default theory below was wrong. Cause is the recorder's **span carry-over**: `_parse_turn` keeps `span_delivered` across turns and `_span_slice` only cuts at the previous `record_moment`; when the agent skips `record_moment` on quiet turns (it did — "Silent." turns), `evals_seen` stops moving and a delivery tagged after a `record_moment` stays "in span" for every later Stop. **Fixed in the agent repo (Track A, `b6c896f`):** the wrapper passes `floor_ts` = the previous record's new `ts_end`; `_span_slice` drops entries at/before it except the current turn's own; same-trigger re-fires are not recorded twice. Regression test reproduces the live ordering. The `reply` `channel:"system"` exclusion stays as it was (it is correct — `channel || 'web'` posts a visible outbound row).
- **Original (superseded) analysis:** `reply` counts as delivery unless `channel:"system"`
- **Where:** `ll5-run-claude-code:.claude/hooks/lib/eval_record.py` (`DELIVERY_TOOLS`, the `channel == "system"` exclusion); `channel/ll5-channel.mjs:566` makes `channel` optional, default `web`.
- **Evidence:** Aug 21: 183 `ping_now` with `message_sent:true`, all one session (`cb38209f`), 100 of them 09:00–11:00Z — vs 24 outbound rows in `ll5_chat_messages` that day, none 08:00–11:00Z. Partial fix of the 2026-07-01 inversion (`HANDOFF.md`, "Eval recorder semantics changed 2026-07-02").
- **Consequence:** `ping_now`, `decision_mismatch`, and `grounding_calls`-on-ping numbers are contaminated; every `behavior.*` alert reads them.
- **Fix shape:** count a `reply` as delivery only when the channel is explicitly user-facing; make `channel` required in the tool schema.

### ISS-002 — `note_observation` near-dead
- **Where:** `packages/ll5-run-shared/CLAUDE.md:353` — "MUST produce a journal entry **or** a narrative `note_observation`". The `or` made the observation optional; journal is the cheaper half.
- **Evidence (audit_log, `tool_name`):** `note_observation` Jun 963 / Jul 435 / Aug 88 / Sep 11; `write_journal` Jun 3,673 / Jul 6,954 / Aug 9,020. Aug 23–31: zero observations. `docs/design/narratives.md:228` specifies both are written together.
- **Fix shape:** split the `or`; nightly `consolidate` emits a machine-readable tally; one `knowledge.observations_stale` anomaly check.
- **2026-09-04 Phase 2 shipped:** `packages/ll5-run-shared/CLAUDE.md` default-write rule now reads "a journal entry — **and**, whenever the event names a person/place/group/topic/mood/preference/plan/decision, ALSO a `note_observation`" (with the field names, since 2 of 13 calls failed validation — ISS-021); `consolidate` Step 1.4 writes observations for every subject the day named that has none; Step 5.3 journals a `CONSOLIDATE-TALLY …` line; `knowledge.observations_stale` (24h) in the anomaly monitor. The Track B persona text (ISS-021: `create_tickler` field names, `upsert_fact` shape, Slack via `query_im_messages`) applied in the same edit. Ships on the run-claude rebuild — persona changes now rebuild the agent (workflow: `packages/ll5-run-shared/` → `run-claude`).

### ISS-003 — Narrative consolidation silent 12 days
- **Where:** `packages/personal-knowledge/src/repositories/elasticsearch/narrative.repository.ts:420` `selectConsolidationWork` — a narrative is due only when a new observation lands in the 14-day window.
- **Evidence:** `consolidate_narrative`/`upsert_narrative` fired only Aug 21, Aug 22, Sep 4. ~1,000 loop runs logged "Refreshed 0, created 0 … Nothing due" — correct given ISS-002. `last_consolidated_at` by month: Jun 130, Jul 68, Aug 19, Sep 3.
- **Fix shape:** none of its own — resolves with ISS-002. Verify it resumes without touching the loop.

### ISS-004 — `ping_later` books nothing
- **Where:** `channel/ll5-channel.mjs:827` — `record_moment` is pure local instrumentation, no side effect. Only a separate `create_wake`/`create_tickler` makes a deferral real; the recorder merely observes that.
- **Evidence:** `ll5_eval_moments` Aug 21–Sep 4: claimed `ping_later` 72, actual `suppress` 57 (79% hollow). `ping_later` actual = 15 in 15 days; `close_count` sum 4; `pencil_count` sum 14.
- **Fix shape:** `record_moment(decision:"ping_later")` requires a `wake_id`/`tickler_id` (structural); fallback: `behavior.forward_work_stalled` alerts on hollow *rate*.

### ISS-005 — No idempotency on telemetry writes
- **Where:** `packages/gateway/src/server.ts:944` (eval-moment) and `:985` (turn-cost) — bare `esClient.index({ document })`, no `id`, no `op_type`.
- **Consequence:** a retried or double-fired Stop hook double-counts into every rate-shift baseline.
- **Fix shape:** deterministic `_id = ${session_id}:${ts}`, `op_type:'create'`; tests in `__tests__/eval-moment-route.test.ts`.
- **2026-09-04 fixed:** `indexOnce()` in `server.ts` — id `${session_id}:${ts}` + `op_type:'create'`; a 409 returns `{ok:true, duplicate:true}`; no `session_id` → old auto-id append. 5 tests. Caveat: a retry that regenerates `ts` is not deduped (documented, acceptable).

### ISS-006 — `ll5_turn_costs` dead since 2026-07-13
- **Evidence:** index max `timestamp` 2026-07-13T10:18Z; 68 docs total. Writer was the opencode `stop-mirror`; runtime is now the Claude Code variant.
- **Also:** `ll5_turn_costs` and `ll5_reconcile_metrics` have no declared mapping (only `ll5_eval_moments` does, `server.ts:110`).
- **Fix shape:** turn-cost writer for the Claude Code variant; declared mappings; `telemetry.turn_costs_stale` check.
- **2026-09-04 partial:** mappings for `ll5_turn_costs` and `ll5_reconcile_metrics` declared in `GATEWAY_INFRA_INDICES`. Note `ensureIndices` is create-if-missing, so the **existing prod indices keep their dynamic mappings** — this protects fresh deploys/tenants only. The stale check is deliberately **not** added yet: with no writer it would fire immediately and stay red until the agent-repo change ships; it lands together with the writer.

### ISS-007 — Live CLI ≠ pin
- **Evidence:** container `ll5-agent-f08f46b3-…` runs image `ghcr.io/arnonzamir/ll5-run-claude:latest` created 2026-07-15T05:37Z; `/usr/local/lib/node_modules/@anthropic-ai/claude-code/package.json` = **2.1.197**; `ll5-run-claude-code:Dockerfile:70` pins **2.1.204** (bumped 2026-07-08, `ec9f45f6`). Model hard-coded `--model claude-opus-4-7` in `ll5-server:85`.
- **Fix shape:** identify the workflow that builds `ll5-run-claude:latest`; make `docker-entrypoint.sh:186`'s version log an assertion; rotate `TS_AUTHKEY`; one green CI run.
- **2026-09-04 ROOT CAUSE + FIX (DECISION-027):** two images, two pipelines. `ll5-run-claude-code` CI built `ghcr.io/arnonzamir/ll5-agent` (pinned 2.1.204) and deployed it to Coolify app `js8owk0g…` — a zombie (`exited:unhealthy`, 20,026 restarts). The **live** container runs `ll5-run-claude`, built by **ll5** CI from `docker/Dockerfile.ll5-run-claude` with an **unpinned** `npm install -g @anthropic-ai/claude-code` (→ 2.1.197 on Jul 15), only on manual dispatch (last Jul 19) or the weekly schedule — which failed 7/7 since Jul 20 because `build (run-opencode)` 403s on GHCR and fail-fast cancelled `build (run-claude)`. Fix: the ll5 Dockerfile pins the CLI (`ARG CLAUDE_CODE_VERSION`, build-time `claude --version` assertion, `ENV` exported), the agent repo becomes a trigger (`trigger-ll5-rebuild.yml`; its Dockerfile + builder workflow deleted), ll5 builds `run-claude` on that dispatch and on the weekly schedule (opencode dropped from the schedule), the deploy job calls the new orchestrator `POST /runtimes/reprovision-running` so the image actually reaches the per-user containers, the entrypoint asserts the version at boot (critical alert on mismatch), and the zombie Coolify app is deleted. `TS_AUTHKEY` (rotated today) only ever served the deleted path. **Verify:** first dispatch green → `docker exec <agent> claude --version` = 2.1.204 → entrypoint log `claude version OK`. Remaining: delete the `ll5-agent` GHCR package (needs an owner token with `delete:packages`); set `LL5_DISPATCH_PAT` on the agent repo.

### ISS-007 (continued) — first end-to-end dispatch, 2026-09-04 16:01Z
- Agent-repo trigger → ll5 `repository_dispatch` → `build (run-claude)` **green with the tripwire** (`[verify] claude --version -> 2.1.204 (pin 2.1.204)`) → pushed → deploy green → host pulled `latest` (`CLAUDE_CODE_VERSION=2.1.204`, `LANG=en_US.UTF-8`) → deploy called `POST /runtimes/reprovision-running` → orchestrator answered `reprovision skipped, no agent token` → **container not rolled** (still 2.1.197, tmux from Aug 26). The roll step is `|| echo WARNING`, so the job stayed green — correct, but it means "deploy: success" is not "agent rolled"; verify inside the container every time. Cause is ISS-024.

### ISS-024 — Orchestrator could never re-provision on its own
- **Where:** `packages/agent-orchestrator/src/orchestrator.ts:98` `this.agentTokenResolver = deps.agentTokenResolver ?? (async () => null)`; `index.ts` never passed one. Both `reconcile()` (stale-heartbeat restart, `:333`) and `reprovisionRunning()` (`:388`) branch to "no agent token" and skip.
- **Evidence:** orchestrator log 2026-09-04T16:06:01Z `reprovision skipped, no agent token` / `reprovision-running done reprovisioned:0 failed:1`. The restart path has logged `cannot restart, no agent token` since it was written.
- **Fix (2026-09-04):** `SecretsWriter.readAgentToken(userId)` reads `LL5_AGENT_TOKEN` back from the tenant's own `/run/ll5/<uid>.env` (the file the orchestrator writes at provision, 0600), and `index.ts` wires it as the resolver. Caveat: it re-uses the last minted token; if agent tokens ever get an expiry, a re-provision after expiry will launch a container with a dead token — the gateway provision path mints fresh.

### ISS-008 — Reconcile subsystem is a no-op
- **Where:** selector `packages/gateway/src/reconcile.ts:46` (zero-paths at `:57`, `:62`, `:94`, `:103`, `:105`), GTD copy `packages/gtd/src/tools/reconcile.ts:54` (`:79` es-null degrade); governor `scheduler/reconcile-governor.ts:76`.
- **Evidence:** `list_reconcile_work` 682 calls, `reconcile_loop` 0, every `ll5_reconcile_metrics` doc `candidate_count:0`, `reconciliation_coverage:null`, 15 days.
- **Fix shape:** make the zero-paths distinguishable, then fix or retire (DECISION-028).
- **Closed (2026-09-04):** retired under DECISION-028 #1 — gateway selector/gate/governor, GTD tools, the five `reconcile.*`/`loop.reconcile_*` anomaly checks, the `reconcile_confirm` tray kind and `POST /me/reconcile/confirm` are gone from the ll5 repo. GTD columns/migrations and the `ll5_reconcile_metrics` index data stay. The in-container worker is removed by the agent-repo PR.

### ISS-009 — Duplicate policies and selectors
- `scheduler/narrative-consolidation.ts` (default OFF, `promoteThreshold 3 / debounce 6h`) vs `narrative.repository.ts:420` (`1 / 45m`); reconcile selector and gate each exist twice (gateway + gtd).
- **Reconcile half closed (2026-09-04):** both selector+gate copies retired with DECISION-028 #1. The narrative-policy half (retire `NarrativeConsolidationScheduler`, DECISION-028 #2) is another track's.

### ISS-010 — Housekeeping dominates
- **Evidence:** `ll5_audit_log` Aug 21–Sep 4: 27,055 tool calls, 20,700 housekeeping (`list_narratives` 9,350, `write_journal` 4,952, `recall_lessons` 4,009, `list_narrative_work` 1,031, `list_reconcile_work` 682). 32 schedulers in `scheduler/index.ts`.

### ISS-011 — Journal backlog
- **Evidence:** 1,229 `type:context` entries `status:open` created in the last 7 days; `resolve_journal` 467 calls in 15 days; intake ~300/day, nightly consolidation folds 60–85.

### ISS-012 — User-model history dead (was: "Learning flat")
- **Finding (2026-09-04):** not archive-only — a silent write failure. `ll5_agent_user_model_history` was never declared in `awareness/src/setup/indices.ts`, so it was dynamic-mapped; the free-form `content.<person>.<key>` paths reached exactly 1,000 mapped fields (ES default `index.mapping.total_fields.limit`) and the last snapshot landed 2026-06-20T22:34Z. `write_user_model` kept succeeding daily (`ll5_audit_log`: Jun 17, Jul 31, Aug 37, Sep 3 calls, all `active_context`), while every history write since threw `Limit of total fields [1000] has been exceeded` inside `journal.ts` write_user_model's empty `catch {}`. Same pattern in `lessons.ts` `snapshot()` and the `learned_notes` append (which, worse, would overwrite all learned notes with one on any non-404 GET failure). `ll5_agent_lessons_history` is fine (declared mapping, 19 fields, 84 docs; 1 in Aug because only 1 lesson was updated).
- **Fix:** history index recreated with the live index's mapping (`content: {type: object, enabled: false}`) via a count-verified temp copy (148 → 148 docs); declared in `indices.ts`; snapshot failures now `logger.error` + a `warning` field in the `write_user_model` result (the live write still goes through), `created_at` preserved across overwrites, `learned_notes` append aborts on a non-404 GET failure. The lessons-count part ("3 lessons in 15 days") is not a bug — it moves to the 7-day re-measure as a behaviour KPI.

### ISS-013 — Chronic infra
- WA bridge stall on 13 of 15 days (62 journal mentions; Aug 28: 4 criticals, 3 restarts none cleared, self-recovered 1h40m). Gmail mirror listener once ~4.5 days dead; Slack ~29h then ~20h. Google OAuth disconnects Aug 24, 27, Sep 3–4. TS_AUTHKEY rotation wake (Aug 23) **cancelled, not done**. `ll5-run-claude-code` CI last green 2026-07-14.
- **Fix shape:** per source, fix or stop alerting. An alert answered nightly with the same ineffective reflex is worse than none.

### ISS-013 (WhatsApp part) — the "bridge stall" was night silence (fixed 2026-09-05)
- **Evidence:** `ll5_chat_messages` `[ALERT]` deliveries over 14 days: 14 `channel.whatsapp` episodes, every one between 01:07 and 05:21 local, 13 on the flat rule `last inbound 2h ago (expected < 2h)`, 1 on the divergence rule at 01:31 (a phone notification counted as "other channel flowing"). WhatsApp inbound by local hour over 14 days: 01:00–05:00 empty on 7–10 of 14 nights; 08:00–22:00 never empty. Evolution container up since Jul 6 (6 restarts total), only two `stream errored out 503` (Baileys auto-reconnect) in 14 days. `notify_count` on the alert row: 267 lifetime. The agent's restarts never "fixed" anything — the inbox filled when people woke up. The global active-hours guard is 0–24 for every scheduler, so it never protected the night.
- **Fix:** `recordBridgeEvent` in `processors/whatsapp-dispatch.ts` records every Evolution event (messages.update receipts, chats.update, connection.update carry the socket's liveness at night); the monitor's pure policy `evaluateWhatsAppFlow` (tested) ranks signals: `connection.update` state=close > 5 min → stale at any hour; quiet hour (this local hour empty on ≥5 of the last 14 days) → never stale on silence; daytime flat silence > 2h → stale (kept — never happens when healthy); daytime divergence → stale unless an Evolution event arrived within 30 min. Alert text now carries the bridge-event age and a suggestion that does not start with "restart".
- **Not changed:** the messaging MCP's `restart_whatsapp_account` reflex in the persona (harmless once the alert stops); Gmail/Slack mirror listeners and Google OAuth disconnects (separate causes, still open).

### ISS-014 — `POST /sessions` exceeds the 1 MB body cap
- **Where:** `packages/gateway/src/server.ts:363` `express.json({ limit: '1mb' })` (global); `/sessions` handler `:1470`; `ll5-run-claude-code:.claude/hooks/session-save.sh` sends the whole session on every Stop with `curl -sf` (swallows non-2xx); `lib/session_payload.py` caps per-message text at 2,000 chars but not message count.
- **Evidence:** every session freezes at the same point — `2f145954` 174 msgs, `cb38209f` 241, live `6ec9fd1c` 264 (frozen 2026-08-27T06:21Z; transcript now 74,370 lines / 91.8 MB). `ll5_session_history` docs: Jul 1,836, Aug 3.
- **Fix shape:** incremental send (messages after stored `last_message`); log non-2xx to `~/.ll5/`; `agent.session_save_stale` check.
- **2026-09-04 ROOT CAUSE CORRECTED (verified live):** the first wall is in the hook itself, not the gateway. `session-save.sh` passes the payload as one argv string — `curl … -d "$PAYLOAD"` — and Linux caps a single argument at `MAX_ARG_STRLEN` = 131072 bytes. Reproduced in the container: `curl: Argument list too long`, exit 126. 128 KB ≈ 250 messages × ~500 B — exactly the 174/241/264-message freeze points. The gateway's 1 MB cap was the *second* wall behind it. Payload build itself takes 0.9 s for 7,818 messages (no timeout issue). **The agent-side fix is mandatory and blocked on the agent deploy path (ISS-007):** `--data-binary @"$TMPFILE"` (or `-d @-` from stdin) plus `mode:"append"` with a bounded tail, plus logging non-2xx. Until it ships, the hook fails on every Stop; the live doc was unfrozen once by a manual POST (15:25:36Z, 7,818 messages, 2,818 dropped) and will go stale again → `agent.session_save_stale` will fire in 24h, correctly.
- **2026-09-04 (gateway side shipped):** `/sessions` gets a route-scoped `express.json({limit:'10mb'})` (the live 9-day session's full payload measured 3.87 MB / 7,807 messages, so the existing full-overwrite hook resumes saving on its next Stop with no agent change); new `mode:"append"` — caller sends only the tail, gateway keeps stored messages and appends those with `timestamp > stored last_message`, `if_seq_no`/`if_primary_term` guard (409 = retryable), 403 if the stored doc belongs to another tenant. Both modes cap stored messages at 5,000 (oldest dropped, `messages_dropped` counted) and project `transcript_text` from the **newest** 200k chars (it was the oldest 200k — recent turns were never searchable). `agent.session_save_stale` (24h on `indexed_at`, scoped on `user_id.keyword` because the index is dynamic-mapped) added to `anomaly-monitor.ts`. **Remaining (agent repo):** `session-save.sh` sends `mode:"append"` with a bounded tail and logs non-2xx to `~/.ll5/session-save.log`.

### ISS-015 — Re-ground reads the frozen index
- **Where:** `session-start.sh:136` (compact branch) instructs `recent_sessions(days:7)` / `recall_everything({mode:"timeline"})`; both read `ll5_session_history` (`packages/awareness/src/tools/recall-everything.ts:92`).
- **Evidence:** 7 auto-compactions in the live session (Aug 27 18:02 → Sep 4 00:41Z), each re-grounded on the Aug 27 snapshot. Resolves with ISS-014.

### ISS-016 — Sessions unmonitored, roll only on restart
- **Evidence:** live session 9 days; previous 13 days / 136 MB, ended by container restart (`RestartCount=5`). No check in `anomaly-monitor.ts:502` covers session age, compaction, or session-save; zero journal entries mention a compaction.
- **Decision (2026-09-04):** one controlled restart per day, event-triggered on the nightly `consolidate` pass completing (02:45 local fallback), plus a conditional restart if context crosses ~120K. Gated on ISS-014 being verified live.
- **2026-09-04 shipped (agent repo, ll5 anomaly):** the `consolidate` skill's last step is `touch ~/.ll5/restart-requested`; `mcp-autoheal-server.sh` `maybe_fresh_restart()` restarts at the next idle tick (forced after 30 min), also when no fresh start in 26h, or when the last turn's context ≥ 120K tokens; ≥2h between restarts; `ll5-server` relaunches without `--continue` on `~/.ll5/fresh-session`; `session-start.sh` journals `session-restart` (resolved) and tells the agent it's deliberate; `agent.daily_restart_missing` (26h) watches for that entry. Knobs: `LL5_RESTART_IDLE_SECS`, `LL5_RESTART_FORCE_AFTER_SECS`, `LL5_MAX_SESSION_HOURS`, `LL5_CONTEXT_CAP_TOKENS`, `LL5_RESTART_MIN_GAP_SECS`. Manual: `touch ~/.ll5/restart-requested` in the container.

### ISS-017 — Memory-capture path idle
- **Where:** `memory-intercept.sh` (PreToolUse Write|Edit under `*/memory/*` → `ingest_memory`, deny disk write, fail-open); `memory-recall.sh:16` (`recall_lessons` limit 5 on every UserPromptSubmit).
- **Evidence:** `ingest_memory` Jun 36 / Jul 83 / Aug 0 / Sep 0; `upsert_lesson` Aug 7 / Sep 0; `recall_lessons` Aug 7,625. Memory dirs in the container are empty (the intercept works; nothing feeds it).
- **Decision (2026-09-04):** fail-closed with an outbox (`~/.ll5/memory-outbox.jsonl`) drained by the autoheal loop; disk is transport, never a read source.
- **2026-09-04 context pack (DECISION-028 #4, Arnon: "all relevant context, all the time"):** `session-start.sh` now loads ALL active lessons (was 12), plus active narratives, open journal and a last-day timeline on **every** start (they were compaction-only; with the daily restart `startup` is the common path). `memory-recall.sh` rewritten as a per-turn context pack: contact messages → that person's narrative + last observations (`recall` by subject) + ≤3 lessons on the inner text + name; user prompts → ≤3 lessons + a compact `recall_everything` sweep; scheduler/system rows → nothing. Every injection logged to `~/.ll5/context-pack.log` (`cls=… lessons=… person=… sweep=…`) — the measurement for whether per-turn injections are ever cited. Replaces 269 random `recall_lessons` calls/day.

### ISS-018 — ES bypass via Bash over spill files
- **Evidence (live transcript):** 14 `Bash` calls in 9 days; 12 are `grep`/`python3` against `/data/home/.claude/projects/-workspace/<session>/tool-results/mcp-awareness-*.txt` (Aug 27 "Ivgi", Sep 2 "Yishay/ישי", nightly `consolidate` parsing Aug 28 ×5, Aug 30 ×2). Zero `Grep`/`Glob`/`Read`/`WebSearch` calls. 8.1 MB of spill files in the live session, 362 MB under `~/.claude/projects`.
- **Consequence:** no audit row, no `grounding_calls` credit, snapshot not live, wiped on redeploy.
- **Fix shape:** ISS-019 removes the cause; a `PreToolUse` Bash hook denies reads under `.claude/projects` / `tool-results/`.

### ISS-019 — Unbounded MCP read results
- **Evidence:** `mcp-awareness-read_journal-*.txt` 50–61 KB per nightly call; `mcp-awareness-recall_everything-*.txt` 73–114 KB; transcript errors "result (1,698,093 characters …) exceeds maximum allowed tokens" and 700,199 / 238,704 / 232,701 chars.
- **Fix shape:** hard result cap (~20 KB) + cursor pagination + `truncated:true, next_cursor` in `recall-everything.ts`, the journal reader, and personal-knowledge `recall`/`list_narratives`.
- **2026-09-04 fixed.** Paired every oversized result in the live transcript with its call: the 1,698,093-char and 700,199-char results were `recall_everything` with `all_sessions:true` — `results[].data` carried whole `ll5_session_history` docs (`messages` + `transcript_text`); 232,701 / 238,704 were `recall_everything` timeline sweeps; the 50–61 KB nightly ones were `read_journal(status:"open", limit:100)`; `list_narratives(limit:30)` hit 86,900. Shipped one contract in `@ll5/shared` (`packages/shared/src/mcp/result-cap.ts`: `MCP_RESULT_CAP_CHARS = 20_000`, `capItems`, `encodeCursor`/`decodeCursor`, `pageFields`) and applied it to `recall_everything`, `read_journal`, `query_im_messages` (awareness), `recall`, `list_narratives`, `get_narrative` (personal-knowledge), `read_messages` (messaging): truncation at item boundaries, newest kept, `truncated:true + next_cursor + hint` only when something was cut, small results byte-identical. `recall_everything` now strips `messages`/`transcript_text` from session docs (replaced by `message_count`/`transcript_chars`) and clips any single `data` string field at 4,000 chars. `list_narratives`/`get_narrative` accept `max_chars` (≤500 KB) for the gateway/dashboard, which fetch 60–100 items per call and are NOT updated in this change — until they pass `max_chars` or follow `next_cursor`, those views show the first ~20 KB page. Tests: shared +10, awareness +17 (+2 updated), personal-knowledge +18, messaging +5.

### ISS-020 — Deferred tool schemas
- **Evidence:** 13 `ToolSearch` calls in 9 days — 10 in the first 16 h, none Aug 27 12:56 → Sep 2 23:10, then only `push_to_user`/`reply`/messaging after compactions.
- **Decision (2026-09-04):** `session-start.sh` pre-loads a core set on `startup`/`compact`; cut tool count in Phase 4.

### ISS-021 — MCP input-validation failures
- **Evidence:** `MCP error -32602` on `read_messages` ×3, `create_tickler` ×3, `write_journal` ×3, `upsert_fact` ×2, `get_person` ×2, `note_observation` ×2, `upsert_lesson`, `log_habit_outcome`, `link_media`, `list_horizons`, `write_user_model`.
- **Fix shape:** diff each tool's Zod schema against what persona/skills instruct; `note_observation` first.
- **2026-09-04 fixed.** Exact error bodies + the inputs that caused them pulled from the live transcript (19 failures, each stored twice in the JSONL, hence "~20"): `read_messages` ×3 `platform:"slack"`; `create_tickler` ×3 sent `date` / `start`+`end` / `start_date` instead of `due_date`; `write_journal` ×3 — `signal:"consolidated"` ×2 (the **consolidate skill instructs it**) and `type:"confirmed"` ×1 (agent slip; `confirmed` is a signal); `upsert_fact` ×2 sent `{content,tags}` / `{key,value}` without type/category/provenance/confidence; `get_person` ×2 `person_id`; `note_observation` ×2 — `subject` as a JSON **string** + `content`, and `content` + `source:"observation"`; `upsert_lesson` ×1 bare `content`; `log_habit_outcome` ×1 `outcome:"skipped"`; `link_media` ×1 `link_kind/link_type/link_id`; `list_horizons` ×1 `{}` (coach-scan skill reads it as "all levels"). `write_user_model`: **0** failures in the transcript (the register's list was wrong). Schema fixes, each locked by a test on the live payload: `note_observation` accepts `subject` (object/array/JSON string) and `content`, normalizes `source` (`observation`/`photo` → `inference`, `user` → `user_statement`, missing → `inference`) and echoes `normalized`; `write_journal.signal` gains `consolidated` + `completed` (`type` stays strict); `upsert_fact` requires only the text (`content` or `value`; `key` → category) and defaults type/category/provenance/confidence, echoing `defaults_applied`; `get_person` takes `id` or `person_id`; `create_tickler` takes `due_date` | `date` | `start_date` | `start` (ISO datetime → date + wall-clock time); `upsert_lesson` takes `content` (first line → claim, body → detail; trigger defaults to the claim, durability to durable); `log_habit_outcome` maps `skipped`/`skip` → `skipped_deliberate`; `link_media` takes `link_type`/`link_id`; `list_horizons` without `horizon` lists h2–h5; `read_messages.platform` is a string — an unsupported platform returns `UNSUPPORTED_PLATFORM` with the exact `query_im_messages` call to make instead. Persona/skill text changes (not applied here — `packages/ll5-run-shared/` is owned by another track): CLAUDE.md:91 name `due_date`; CLAUDE.md:89 name the `upsert_fact` fields. Tests: awareness (write_journal/upsert_lesson/link_media in `result-cap-tools.test.ts`), personal-knowledge `narrative-tools.test.ts`, gtd `iss-021-schema-fixes.test.ts` +4, google `tools.test.ts` +4, messaging `read-messages-cap.test.ts`.

### ISS-022 — `record_moment` turn tax
- **Evidence:** live transcript: `record_moment` 3,211, `write_journal` 2,032, `resolve_journal` 347, `push_to_user` 207, everything else ~350.
- **Fix shape (Phase 4):** infer `decision_claimed` from a structured line in the assistant's final text instead of a tool round-trip.
- **2026-09-04 (DECISION-028 #6 shipped):** the narrative loop no longer spawns a Sonnet worker on empty ticks (HTTP pre-check of `list_narrative_work`), and the prompt no longer journals empty runs; the dashboard Active-topics rail polls every 5 min (was 45 s) with limit 60 (was 150) and refreshes on tab visibility.

### ISS-023 — Docs-only pushes redeploy everything
- **Where:** `.github/workflows/build-and-push.yml`, `detect-changes` → `set-matrix`, the push branch. `PACKAGES` starts as the full infra list; the per-package filter only replaces it when `${#FILTERED[@]} -gt 0`. A commit that touches no `packages/<pkg>/` path (docs, workflows, root files) matches nothing, the narrowing is skipped, and the full list goes to the build matrix and then to `deploy`.
- **Evidence:** run `33886995650` for `7dbf725` (docs only: `docs/ISSUES.md`, `docs/reviews/…`, PROGRESS/HANDOFF/FILE_TREE) — `build (…)` ran for all 10 packages and `deploy` ran, all green. Every docs-only commit since this logic landed has done the same: a full-stack restart per doc edit.
- **Fix shape (one line):** `else PACKAGES=(); fi` after the `-gt 0` check, so nothing-matched → empty matrix. Then confirm the `deploy` job's `if:`/`needs:` skips cleanly on an empty matrix for a push (it already does for `repository_dispatch`, whose comment explains the empty-string convention) — a docs-only push should build nothing **and deploy nothing**.
- **2026-09-04 fixed** (bundled with DECISION-027): `else PACKAGES=()` in `set-matrix`, and the deploy job's `if:` gains `(github.event_name != 'push' || needs.detect-changes.outputs.matrix != '')`. Verify: the next docs-only push shows `detect-changes` green, no `build (…)` jobs, `deploy` skipped.

---

### ISS-029 — Parked on a startup picker after a forced relaunch (2026-09-05 11:21–12:00Z)
- **What happened:** the new post-roll smoke (DECISION-029) killed claude with the `fresh-session` flag to prove a relaunch works. `ll5-server` relaunched and ran `dismiss_startup_prompts`; the loop's first poll saw `shift+tab to cycle` on the pane — the dead instance's last screen, not the new one — and exited ("picker dismissal done after 1 polls"). The dev-channels picker then drew and stayed. The smoke's `agent_up` matched the same stale line and reported "forced relaunch OK after 10s". Process up, heartbeat `claude_alive: true`, no turn for 40 minutes; 8 scheduler triggers pending and a user message (11:56) unanswered until I pressed Enter by hand at 12:00Z.
- **Fix:** `ll5-server` clears the screen (`printf '\033[2J\033[H'`, `tmux clear-history`) before launching claude; both dismissers handle a visible picker BEFORE testing for the chat input and stop only after two consecutive clean polls; the deploy smoke treats a visible `Enter to confirm` as not-up; the heartbeat reports `picker_visible` and the gateway raises `agent.picker_stuck` (critical) after 3 minutes (`agent_runtimes.picker_since`, migration 047).
- **Lesson:** a screen is not a state. Every check that reads the tmux pane must first look for the thing that blocks (a prompt) and only then for the thing that proves readiness.

### ISS-028 — WhatsApp read empty + messaging audit disabled (2026-09-05)
- **Report (from the agent, relayed by Arnon):** messaging `read_messages` for WhatsApp returns disconnected, then empty, while the account reports connected and ingestion (Evolution → RabbitMQ → awareness mirror) is fine; the agent reads via `query_im_messages` instead; sending unverified.
- **Findings:** Evolution instance `ll5` state `open`, 307,387 stored messages, latest ~07:38Z; the account row `connected` (updated 07:31Z — "disconnected" was the real 22:07–03:37Z bridge stall, ISS-013). `EvolutionClient.fetchMessages` did `result.messages ?? []` and `read_messages` then `Array.isArray(...) ? ... : []` — but v2 returns `{ messages: { total, pages, currentPage, records: [...] } }` (as `fetchMessagesPaginated` already knew), so every WhatsApp read was `[]` with no error. Separately, the messaging service in `docker/docker-compose.prod.yml` had no `ELASTICSEARCH_URL`: `initAudit('')` and every `logAudit` call went nowhere — `ll5_audit_log` has no `source: messaging` rows at all, which is also why the agent's calls left no trace to diagnose from.
- **Fix:** `fetchMessages` accepts `{messages:{records}}`, `{messages:[...]}` and raw arrays (tests in `evolution-client.test.ts`); `ELASTICSEARCH_URL` added to the messaging service; `initAudit('')` logs `[audit] ELASTICSEARCH_URL is empty — audit logging is DISABLED` at boot. Send path (`send_whatsapp` → Evolution `sendText`) untouched and untested here — verify with a real send when convenient.
- **Follow-up:** `packages/messaging/src/__tests__/contact-settings-tools.test.ts` has 3 pre-existing failures unrelated to this change (fail on a clean checkout too).

### ISS-027 — Startup-picker exit loop after the 2.1.260 upgrade (2026-09-05)
- **Timeline (UTC):** 00:37 Phase 5 roll (2.1.260 + opus-5) → worked; 01:30 worker-model roll → worked; 03:43:34 watcher cap restart (`context 170027 >= cap 150000`) → `ll5-server` relaunch with `dismiss_startup_prompts` → `Down Enter` → dev-channels picker (`❯ 1. I am using this for local development / 2. Exit`) took Exit → `[ll5-server] claude exited on its own — ending session for container restart` → entrypoint restart → its own `Down Enter` (written for the first-boot "No, exit / Yes, I accept" picker, which no longer appears on a restart) → same exit → loop every ~100 s until the hot-patch at 07:19:57; claude back 07:20:43, fixed image rolled 07:23:20. Last journal before: 03:41:11; first after: 07:20:36 (`session-restart`).
- **Why the two boots after the upgrade survived:** a reprovision wipes `/data/home`, so the first-run accept picker appears first and absorbs the `Down`; on a plain restart it is already answered.
- **Why nobody noticed for 3.5h:** the container heartbeat (`POST /me/agent/heartbeat` `{}`) proved only that the container was up — it beat through the whole loop, so the orchestrator saw `running`. `agent.output` (silence inference, 15-min ticks, ≥0.5h silence + journal-alive 2h floor, active hours only) raised at 05:42Z; `loop.narrative_consolidation` at 05:12Z; `throughput.inbound_messages` at 07:12Z (Saturday-morning volume + symptom). The user's push arrived ~08:44 local (alert re-notify policy). All three were true positives caused by one root cause.
- **Fix (agent `0fa8ea7`):** both dismissers read the tmux pane, move the pointer off any Exit-ish line (Down, then Up if still on Exit), press Enter, and stop when the chat input is visible. Runbook check for tomorrow: `docker logs … | grep "picker dismissal done"` after any relaunch.
- **Liveness follow-up (this commit):** heartbeat body `{claude_alive, claude_uptime_s, launches_10m, session_id}` (entrypoint computes; `ll5-server` appends each launch to `~/.ll5/launches.log`); gateway stores it on `agent_runtimes.health/health_at/claude_down_since` (migration 045) and raises `agent.process_down` (critical, 3 min down) / `agent.launch_loop` (critical, ≥3 launches in 10 min) straight from the heartbeat handler — detection in ~3-4 min instead of 2h. `suppressedBy` on symptom checks: `loop.narrative_consolidation`, `agent.journaling`, `agent.daily_restart_missing` ← process_down/launch_loop; `throughput.inbound_messages` ← `channel.whatsapp`/`channel.phone`/`channel.slack`; `agent.output` stays quiet while a liveness alert fires. One root cause → one alert.
- **Not changed (needs data):** `throughput.inbound_messages` weekend sensitivity (3 vs a 30 median at 08–10 Sat local); `agent.output` window (0.5h silence / 15-min tick) — with process liveness in place it can stay slow.

### ISS-026 — Context-cap restarts every ~2h (2026-09-05 checkpoint)
- **Evidence:** watcher log `FRESH RESTART (context 166097 tokens >= cap 120000)` at 20:27Z and `159497` at 22:32Z on 2026-09-04. Transcript profile of session `97a2572e` (2h life): ctx 80,065 after the first turn (cache write 76,759 on the first assistant message), 159,497 at message 94; 9 user turns; tool results only 55 / 90 KB total — growth is many small messages on an 80K floor, not big results. The nightly `restart-requested` flag (23:05Z) waited behind `MIN_GAP_SECS=7200` from the 22:32Z cap restart (flag preserved — `rm -f` only inside `do_fresh_restart`).
- **Decision (mid-level, Arnon's standing authorization for the checkpoint):** `CONTEXT_CAP_TOKENS` 120K → 150K (~70K working room; the watcher killed sessions at 159–166K without auto-compaction having fired on 2.1.204, so 150K stays below that observed point), `MIN_GAP_SECS` 7200 → 3600, and `context_tokens()` adds `cache_creation_input_tokens` (reviewer finding: the measured number omitted the turn's cache write, so real context was always above it — small in steady state, tens of thousands right after a pack refresh). Bundled into the Phase 5 roll (config-only, revertible via `LL5_CONTEXT_CAP_TOKENS` / `LL5_RESTART_MIN_GAP_SECS` env without a roll). Persona and start pack untouched — trimming the 80K floor is Arnon's call (his requirement: all relevant context all the time); options for him: lessons limit 100 → top-N, drop the last-day timeline from the start pack (the per-turn pack covers it).
- **Reviewer verdict (one general-purpose subagent, 2026-09-05 03:3x IDT):** yes to cap + gap + bundling + 2.1.260; not critical; biggest risk = the undercount above (fixed in the same roll); auto-compaction threshold on 2.1.260 is unknown — check `isCompactSummary` on the 09-06 checkpoint; the 56-version CLI jump is the real upgrade risk — verify every hook fires post-roll (2026-07-13 unwired-hooks incident), and that `--model claude-opus-5` resolves.
- **Cost note (not a driver):** since 2026-09-04T18:00Z `ll5_turn_costs` = 29 turns / $43 API-equivalent (a 2h session ≈ $18, cache writes dominate). Longer sessions trade one cold start per ~4h for a bigger read context per turn; the change is justified on working memory, not money. First day with cost data at all — the 7-day readout is the comparison.

## Folded from the old PROGRESS `## Known Issues` (2026-09-04)

| ID | Title | Status |
|---|---|---|
| ISS-K01 | Evolution API `findContacts({where:{}})` times out on 2,913 contacts — single-JID queries work | open (workaround in use) |
| ISS-K02 | Most messaging contacts lack display names — Evolution only provides `pushName`; Android address-book push enriches (needs READ_CONTACTS grant + first sync) | open (mitigated) |
| ISS-K03 | Dashboard MCP client sometimes returns stale responses (needs cache-busting) | open |
